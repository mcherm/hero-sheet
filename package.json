{
  "name": "hero-sheet",
  "version": "0.3.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "validate": "npx ajv -s src/schema/samplePowers.schema.json -d src/data/samplePowers.json && npx ajv -s src/schema/standardPowers.schema.json -d src/data/standardPowers.json",
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "deploy": "s3-easy-deploy --public-root ./dist --bucket hero-sheet.com"
  },
  "keywords": [],
  "author": "Michael Chermside",
  "license": "ISC",
  "dependencies": {
    "s3-easy-deploy": "^1.1.0",
    "vue": "^2.6.11"
  },
  "postcss": {},
  "devDependencies": {
    "@vue/cli-service": "^4.3.1",
    "ajv-cli": "^3.1.0",
    "vue-template-compiler": "^2.6.11"
  }
}
